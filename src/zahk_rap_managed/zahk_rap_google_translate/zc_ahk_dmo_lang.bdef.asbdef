projection;
strict ( 2 );
use draft;

define behavior for ZC_AHK_DMO_LANG
use etag

{
  use create;
  use update;
  use delete;

  use action TranslateText;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}